source_config = {
    "dbname": "source_db",
    "user": "postgres",
    "password": "secret",
    "host": "source_postgres",
}

target_config = {
    "dbname": "target_db",
    "user": "postgres",
    "password": "secret",
    "host": "target_postgres",
}
